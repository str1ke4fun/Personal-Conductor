# 当前架构说明：Conductor 桌宠、工具、记忆与工作区

> 日期：2026-05-26
> 目标：说明当前系统真实架构，并用流程图展示用户进入后的主要操作路径。

## 1. 架构总览

Conductor 当前是一个 Tauri 桌面应用，前端负责桌宠窗口、任务面板、对话面板和设置面板；Rust 后端负责感知、任务、提案、工具执行、记忆、形象切换和 SQLite 持久化。

```mermaid
flowchart TB
    User[用户]
    Pet[桌宠窗口 PetWindow]
    Chat[对话面板 ChatPanel]
    Task[任务面板 TaskPanelContent]
    Settings[设置面板 SettingsPanel]
    IPC[TypeScript IPC invoke.ts]
    Commands[Tauri Commands]
    Worker[后台 Worker / 感知监听]
    Core[conductor-core]
    DB[(SQLite state)]
    Files[(state sidecar files)]
    Sense[conductor-sense]
    LLM[LLM / Claude 子进程]

    User --> Pet
    Pet --> Chat
    Pet --> Task
    Pet --> Settings
    Chat --> IPC
    Task --> IPC
    Settings --> IPC
    IPC --> Commands
    Commands --> Core
    Worker --> Sense
    Worker --> Core
    Core --> DB
    Core --> Files
    Core --> LLM
```

核心分层：

- 前端层：`apps/desktop/src/windows/*`、`apps/desktop/src/ipc/invoke.ts`。
- Tauri 命令层：`apps/desktop/src-tauri/src/commands.rs`。
- 后台感知层：`apps/desktop/src-tauri/src/worker.rs`、`conductor-sense`（焦点/空闲检测）。
- 核心业务层：`crates/conductor-core/src/*`：
  - `chat.rs` — 对话 + LLM 调用 + 工具循环
  - `expression.rs` — 情绪状态机 (7 MoodZone, PAD 模型, 60s 衰减)
  - `affection.rs` — 好感度系统 (5 关系阶段, 迟滞, 每日衰减)
  - `persona.rs` — 人格特质 + prompt 模板 (已注入 system prompt)
  - `smart_monitor.rs` — LLM 驱动的智能监控决策器
  - `initiative.rs` — 主动触发引擎 (文档/编码/事件/时间)
  - `memory.rs` — 记忆系统 (fastembed BGE-small-en-v1.5 向量搜索)
  - `llm.rs` — LLM 调用 (OpenAI 兼容 / Anthropic 兼容)
  - `tools.rs` — 工具注册与执行
  - `tasks.rs` — 任务管理
- CLI/Hook 层：`crates/conductor-cli/src/main.rs`。
- 持久化层：SQLite 表（chat_messages, tasks, mood_state, affection_state, memory_chunks）和 sidecar 文件。

## 2. 用户进入后的操作流程

### 2.1 启动与桌宠进入

```mermaid
flowchart TD
    A[用户启动桌面应用]
    B[Tauri main.rs 初始化]
    C[注册插件和 invoke_handler]
    D[创建托盘菜单]
    E[启动 worker 感知循环]
    F[启动 cursor watcher]
    G[恢复桌宠窗口位置和大小]
    H[显示 PetWindow]
    I[AvatarRenderer 读取设置和当前 AvatarId]
    J[用户看到桌宠并可拖动/缩放/锁定/静音]

    A --> B --> C --> D --> E --> F --> G --> H --> I --> J
```

关键代码：

- `apps/desktop/src-tauri/src/main.rs`
- `apps/desktop/src-tauri/src/worker.rs`
- `apps/desktop/src/windows/PetWindow.tsx`
- `apps/desktop/src/windows/AvatarRenderer.tsx`

### 2.2 用户打开面板

```mermaid
flowchart LR
    User[用户]
    Pet[PetWindow]
    Shortcut[全局快捷键]
    Tray[系统托盘]
    Task[Tasks 窗口]
    Chat[Chat 窗口]
    Settings[Settings 窗口]

    User --> Pet
    User --> Shortcut
    User --> Tray
    Pet --> Task
    Pet --> Chat
    Pet --> Settings
    Shortcut --> Task
    Shortcut --> Chat
    Tray --> Task
    Tray --> Chat
    Tray --> Settings
```

当前快捷键：

- `CmdOrCtrl+Shift+L`: 打开任务面板。
- `CmdOrCtrl+Shift+C`: 打开对话面板。
- 专注模式通过托盘菜单触发（无快捷键）。

## 3. 对话路径

```mermaid
sequenceDiagram
    participant U as 用户
    participant Pet as PetWindow/ChatPanel
    participant IPC as invoke.ts
    participant Cmd as commands.rs
    participant Chat as conductor-core chat
    participant LLM as LLM 客户端
    participant DB as SQLite

    U->>Pet: 输入消息
    Pet->>IPC: api.sendChatMessage(content)
    IPC->>Cmd: send_chat_message
    Cmd->>Chat: 保存用户消息并生成回复
    Chat->>LLM: 调用配置的模型
    LLM-->>Chat: 返回助手回复
    Chat->>DB: 写入 chat_messages
    Chat-->>Cmd: ChatMessage
    Cmd-->>IPC: ChatMessage
    IPC-->>Pet: 展示回复
```

当前特点：

- PetWindow 可直接发送轻量消息。
- ChatPanel 读取历史并发送完整对话。
- chat 历史落入 SQLite。
- 回复失败时前端只展示简短错误，不阻断桌宠窗口。

## 4. 任务与提案路径

```mermaid
flowchart TD
    A[Hook / Worker / Chat 生成任务或提案]
    B[写入 tasks 或 agent_tasks]
    C[写入 action_proposals]
    D[Worker 发出 tasks_changed / taskpanel_banner]
    E[TaskPanelContent 刷新列表]
    F{用户选择}
    G[Approve]
    H[Reject]
    I[Execute]
    J[ToolRegistry 查找工具]
    K[Workspace 权限检查]
    L[执行 ToolExecutor]
    M[写入 tool_runs 和 result_ref]
    N[提案状态 succeeded/failed]

    A --> B
    A --> C
    B --> D
    C --> D
    D --> E
    E --> F
    F --> G --> I
    F --> H
    I --> J --> K --> L --> M --> N --> E
```

提案状态机：

```mermaid
stateDiagram-v2
    [*] --> Pending
    Pending --> Approved: approve
    Pending --> Rejected: reject
    Pending --> Expired: timeout/future policy
    Approved --> Running: execute
    Running --> Succeeded: tool ok
    Running --> Failed: tool error
    Succeeded --> Used: consume result
```

关键代码：

- `crates/conductor-core/src/proposals.rs`
- `crates/conductor-core/src/tools.rs`
- `apps/desktop/src/windows/TaskPanelContent.tsx`
- `apps/desktop/src-tauri/src/commands.rs`

## 5. 工具执行架构

```mermaid
flowchart TB
    Proposal[action_proposals]
    Registry[ToolRegistry]
    Spec[ToolSpec]
    Executor[ToolExecutor]
    Internal[Internal Tools]
    Subagent[Subagent / Claude -p]
    AgentRun[Agent Runs]
    Team[Agent Team / Mailbox]
    MCP[MCP Mapping]
    Office[Office Placeholders]
    Runs[tool_runs]

    Proposal --> Registry
    Registry --> Spec
    Spec --> Executor
    Executor --> Internal
    Executor --> Subagent
    Executor --> AgentRun
    Executor --> Team
    Executor --> MCP
    Executor --> Office
    Executor --> Runs
```

当前工具 provider 的真实边界：

- `internal`: demo、task、avatar、tool.search 等已可用。
- `subagent`: `subagent.claude_p` 可同步调用。
- `agent`: `agent.start/read_output/stop` 可用，但长跑由宿主进程托管。
- `team/mailbox`: 轻量持久化可用，不含自动调度。
- `mcp`: mapping/discovery scaffolding 已有，远程 execute 仍待完善。
- `office`: 只有只读/预览占位，没有真实 `office-cli`。

## 6. Workspace 权限路径

```mermaid
flowchart TD
    A[工具调用请求]
    B{是否带 workspace_id}
    C[读取 Workspace]
    D[无 workspace 上下文]
    E{TrustLevel}
    F[trusted: 允许低风险工具]
    G[ask_write: 写操作走提案]
    H[read_only: 阻断写工具]
    I[untrusted: 更保守阻断]
    J[记录执行结果]

    A --> B
    B -->|是| C --> E
    B -->|否| D --> E
    E --> F --> J
    E --> G --> J
    E --> H
    E --> I
```

当前工作区的产品表达仍应是“当前项目/当前文档/当前上下文”，不要暴露成“每个目录都有一个桌宠副本”。

## 7. 记忆架构

```mermaid
flowchart LR
    Chat[对话]
    Summary[ConversationSummary]
    Memory[MemoryEntry]
    Chunk[MemoryChunk]
    Embedding[MemoryEmbedding]
    Search[本地检索与评分]
    DB[(SQLite)]

    Chat --> Summary --> DB
    Chat --> Memory --> DB
    Memory --> Chunk --> Embedding --> DB
    DB --> Search
```

当前实现：

- SQLite 是事实源。
- 对话摘要、用户偏好、工作区记忆可持久化。
- 检索为本地轻量/伪 embedding + 多维评分。

未实现：

- 真正的 embedding provider。
- 记忆可视化管理。
- 敏感记忆审批和过期策略。

## 8. 形象与主动提示路径

```mermaid
sequenceDiagram
    participant Tool as pet.set_avatar / MCP wrapper
    participant Core as avatar.rs
    participant DB as SQLite avatar_state
    participant Cmd as commands.rs
    participant UI as AvatarRenderer

    Tool->>Core: set_avatar(AvatarId)
    Core->>DB: 保存 avatar_state
    Core-->>Cmd: AvatarState
    Cmd->>UI: emit pet_avatar_changed
    UI->>UI: 切换 original/document_secretary/programmer
```

主动提示路径：

```mermaid
flowchart TD
    Focus[前台窗口/进程/空闲状态]
    Worker[worker.rs]
    Events[events log]
    Initiative[initiative.rs]
    Proposal[主动建议或气泡]
    UI[PetWindow / ChatBubble / TaskPanel]

    Focus --> Worker
    Worker --> Events
    Worker --> Initiative
    Initiative --> Proposal
    Proposal --> UI
```

## 9. Agent Run 与 Team 路径

```mermaid
flowchart TD
    A[用户或提案启动 agent.start]
    B[创建 agent_runs]
    C[宿主进程启动 claude -p]
    D[stdout/stderr 写入 state/agent-runs]
    E[用户 read_output]
    F[用户 stop]

    A --> B --> C --> D --> E
    B --> F
```

```mermaid
flowchart TD
    A[创建 Agent Team]
    B[添加 member]
    C[发送 mailbox message]
    D{是否指定 recipient}
    E[写入单条消息]
    F[按成员广播多条消息]
    G[读取 mailbox]
    H[mark_read]

    A --> B --> C --> D
    D -->|是| E --> G --> H
    D -->|否| F --> G --> H
```

当前边界：

- Agent Team 是轻量容器和 mailbox。
- 不负责自动调度、远程执行或跨进程协作。

## 10. 当前最关键的未完成架构点

1. Office real provider：把 `office.*` 从占位替换为真实 `office-cli` 或其他可靠文档解析工具。
2. Worktree sandbox：高风险文件修改先进入临时副本。
3. Agent daemon：把长跑 agent 从宿主进程托管升级为可恢复 worker。
4. Proposal preview UI：展示 dry-run 输出、风险、workspace、工具输入。
5. Memory governance：记忆查看、删除、敏感标记、过期策略。
6. MCP runtime：补齐 provider client、远程调用、连接状态和错误映射。
